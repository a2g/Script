{
  "$schema": "http://json-schema.org/draft-07/schema",
  "description": "a list of specific objects that gets references from mansion.gates.schema.json",
  "type": "object",
  "definitions": {
    "char_type": {
      "enum": ["char_sam"]
    },
    "inv_type": {
      "enum": [
        "inv_sunglasses",
        "inv_motorbike_keys",
        "inv_frozen_steak",
        "inv_thawed_out_raw_steak",
        "inv_carton_of_milk"
      ]
    },
    "prop_type": {
      "enum": [
        "prop_blind(closed)",
        "prop_blind(open)",
        "prop_window(closed)",
        "prop_window(open)",
        "prop_Michael(floating-in-the-distance)",
        "prop_telephone_with_long_cord",
        "prop_Michael(standing-in-your-room)",
        "prop_Michael(sleepy-on-your-bed)",
        "prop_Michael_with_keys(asleep-on-your-bed)",
        "prop_Michael(asleep-on-your-bed)",
        "prop_motorbike",
        "prop_fridge(closed)",
        "prop_fridge(open)",
        "prop_freezer(closed)",
        "prop_freezer(open)",
        "prop_carton_of_milk",
        "prop_frozen_steak",
        "prop_microwave_oven",
        "prop_sunglasses"
      ]
    },
    "goal_type": {
      "enum": ["goal_win"]
    }
  }
}
